type Human = {
    id?: number,
    name?: string,
    status?: string,
    species?: string,
    gender?: string,
    image?: string,
    location?: {
        name?: string
    }
}
export type {Human}